# Vendored subset of the Stage-1 discovery pipeline (arXiv fetch + utils).
# Copied verbatim from the upstream research pipeline; resolved via AUTO_RESEARCH_ROOT
# (falls back to this vendored copy when the env var is unset).
